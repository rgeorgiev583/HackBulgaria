#include <stdio.h>
#include <string.h>

int main(int argc, char* argv)
{
    size_t pointer = (size_t) main, length = 10000, i;
    char byte;
    for (size_t i = pointer; i < pointer + length; i++)
    {

    }
}
