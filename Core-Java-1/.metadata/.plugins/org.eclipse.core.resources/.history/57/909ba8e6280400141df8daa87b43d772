package com.hackbulgaria.corejava.asciiplayer;

import javax.imageio.ImageIO;
import javax.imageio.ImageReader;
import javax.imageio.stream.ImageInputStream;

public class GifPlayer extends SequencePlayer {
	public GifPlayer(String path) {
		super(path);
		
		ImageReader ireader = (ImageReader) ImageIO.getImageReadersByFormatName("gif").next();
		ImageInputStream iis = ImageIO.createImageInputStream(file);
		Input(iis) = fail();
		ireader.getNumImages();
=	}
}
